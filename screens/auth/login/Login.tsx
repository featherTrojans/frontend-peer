import { LoginContainer, LoginText } from "./Login.styles";

const Login = () => {
  return (
    <LoginContainer>
      <LoginText>This is the login</LoginText>
    </LoginContainer>
  );
};

export default Login;
