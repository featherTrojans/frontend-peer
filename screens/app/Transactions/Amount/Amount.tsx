import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

const Amount = () => {
  return (
    <View>
      <Text>This is the amount screen</Text>
    </View>
  );
};

export default Amount;

const styles = StyleSheet.create({});
