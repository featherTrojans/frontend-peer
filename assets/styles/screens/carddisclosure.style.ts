import { StyleSheet } from "react-native";
import { FONTS, fontsize } from "../../../constants";

export const CarddisclosureScreenStyles = StyleSheet.create({
  texttitle: {
    ...fontsize.bbsmall,
    ...FONTS.semibold,
    textAlign: "center",
  },
});
